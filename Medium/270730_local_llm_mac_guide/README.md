# Medium export package

This package contains:
- `local_llm_mac_guide_medium.md`: the article converted to Markdown;
- `media/`: all PNG assets referenced by the Markdown file, including one PNG per original figure, one PNG per formula, and one PNG per table.

The Markdown is designed for environments like Medium that do not support LaTeX formulas or Markdown tables.
